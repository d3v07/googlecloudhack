# secrets — never commit
.env
.env.*
!.env.example
.secrets/
*-key.json
*-sa.json
*.pem

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.uv/
.coverage
.coverage.*
htmlcov/
coverage.xml

# generated evidence packs (served locally via PACKS_DIR) — root-anchored so it
# does not also ignore dashboard/app/runs/ (the /runs/[run_id] route)
/runs/

# node
node_modules/

# os
.DS_Store

# internal — kept local, not pushed to the shared repo
HANDOFF.md
docs/*
# safety-boundary decisions are a tracked governance artifact; the rest of docs/ stays local
!docs/safety-boundary-decisions.md
mongo-build-plan.md

# local agent worktrees / tooling state
.claude/
.codex/
.gstack/

# local design reference images (visual specs, not pushed)
design-refs/
